# Normalize all text files to LF in the repo and on checkout (consistent across
# Windows dev and Linux CI/deploy; avoids CRLF diff noise).
* text=auto eol=lf

# Treat these as binary — never normalize or diff as text.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.woff binary
*.woff2 binary
